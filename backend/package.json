{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend WebSocket server for live translation",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "build:railway": "cd .. && npm install && cd backend && npm run build",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "ws": "^8.16.0",
    "openai": "^4.20.0",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.10",
    "@types/uuid": "^9.0.7",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}

